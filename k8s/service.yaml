apiVersion: v1
kind: Service
metadata:
  name: egovframe-portal-site
  labels:
    app: egovframe-portal-site
spec:
  type: NodePort
  selector:
    app: egovframe-portal-site
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30061
